import { AppRegistry } from 'react-native';
import App from './App'; // Import your main component (replace 'App' with your actual component name)

// Register the main component of your app
AppRegistry.registerComponent('parkpulse', () => App);
